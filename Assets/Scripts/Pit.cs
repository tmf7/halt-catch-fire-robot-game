using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pit : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		// freeze anything over the pit and shrink its x/y scale over time
		// also change its base color (blend it to black)
	}
}
